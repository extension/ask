<h1>Experts in '<%= @location.name %>' (<%= @expert_total_count %>)   <span class='serious-small'><%= link_to 'CSV Email Download', experts_email_csv_expert_location_path(@location, format: "csv") %></span>
</h1>
<h3><%= link_to "View everything in '#{@location.name}'", expert_location_path(:id => @location.id) %></h3>

<%= render partial: 'expert/shared/expert_list' %>

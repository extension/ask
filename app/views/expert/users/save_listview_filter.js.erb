// $('#filtered_question_list').html('<%= escape_javascript(render "/expert/questions/list_of_questions", :questions => @recent_questions) %>');
<% if !params[:location_id].nil? %>
  <% if !params[:location_id].blank? %> 
    $('#county_list').html('<%= escape_javascript(render "/expert/home/counties", :location_obj => @location_pref) %>');
  <% else %>
    $('#county_list').html('');
  <% end %>
<% end %>
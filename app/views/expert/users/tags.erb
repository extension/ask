<h1><%= link_expert_user_avatar(@user, :thumb) %> <%= link_to @user.name, expert_user_path(@user.id) %> answers tagged with '<%= @tag.name %>'</h1>

<p><%= link_to 'See which experts have the tag "' + @tag.name + '"', expert_home_experts_path(:tag_id => @tag.id) %>

<% if @questions.length > 0 %>
  <table>
    <tr>
      <th>Status</th>
      <th>Question</th>  
    </tr>
  <% @questions.each do |question| %>
    <tr>
      <td><%= question.status %></td>
      <td>
        <% # ToDo: Override title method in question to take care of this instead of repeating this logic. %>
        <% if question.title.present? %>
          <%= format_text_for_display(question.title) %>
        <% else %>
          <%= link_to format_text_for_display(question.body.truncate(80, separator: ' ')), expert_question_path(question) %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </table>
<% else %>
  <p>There are no questions in this group with this tag</p>
<% end %>
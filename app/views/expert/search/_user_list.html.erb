<div id="main" class="span12">
   
  <% @users.each do |user| %>
    <p>
     <%= link_to expert_user_path(user) do %>
     <%= get_avatar(user, :thumb) %> <%= link_to user.name, expert_user_path(user) %>
     <% end %>
    </p>
  <% end %>
  
</div>

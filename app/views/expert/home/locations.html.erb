<div id="main" class="span8">
  
  <p><%= select_tag(:location_id, options_for_select(get_location_options), {:prompt => "Pick a location"}) %></p>
  
  <div class="map"></div>
  <h1><%= @location.name %></h1>
  
  <br class="clearing" />
  <div class="span5">
    <div class="location_section">
      <h2>Experts (<%= number_with_delimiter(@expert_total_count, :delimiter => ',') %>)</h2>
    <% if @experts.size > 0 %>
      <% @experts.each do |member| %>
        <p><%= link_expert_user_avatar(member, :thumb) %> <%= link_expert_user(member) %> <br /><span class="downplay">Last active: <%= time_ago_in_words(member.last_active_at) %> ago</span></p>
        
      <% end %>
      <p>view all</p> 
    <% else %>
      <p>There are no experts in this location</p>
    <% end %>
    </div>
  </div>
  
  <div class="span5">
    <div class="location_section">
      <h2>Groups (<%= number_with_delimiter(@group_total_count, :delimiter => ',') %>)</h2>
    <% if @groups.size > 0 %>
      <% @groups.each do |group| %>
        <p><%= link_expert_group_avatar(group, :thumb) %> <%= link_to group.name, expert_group_path(group.id) %></p>
      <% end %>
      <p>view all</p> 
    <% else %>
      <p>There are no groups in this location</p>
    <% end %>
    </div>
  </div>

  <div class="location_section">
    <h2>Questions (<%= number_with_delimiter(@question_total_count, :delimiter => ',') %>)</h2>

    <% if @questions.size > 0 %>
      <% @questions.each do |question| %>
        <%= render :partial => 'expert/shared/title_question', :locals => {:question => question, :show_assignments => false} %>
      <% end %>

    <% else %>
      <p>There are no questions in this location</p>
    <% end %>
  </div>

</div>

<div id="secondary" class="span4">
  <h2>Counties</h2>
  <ul class="county_list">
  <% @counties.each do |county| %>
    <li><span class="tag tag-geography"><%= link_to "#{county.name}", expert_view_county_path(county.id) %></span></li>
  <% end %>
  </ul>
</div>

<script type="text/javascript">
  $("#location_id").change(function() {
    var location_id = $('select#location_id :selected').val();
    window.location = '/expert/home/locations/' + location_id
  });
</script>
<%= form_tag('/expert/questions/activity_notificationprefs') do %>
<%= hidden_field_tag('question', @question.id) %>
  <%- if !current_user.send_activity_notification?(@question) -%>    
    <p><span class="notifylink"><i class="icon-envelope"></i><%= submit_tag "Notify me when this question has activity", {:id => '1', :class => 'btn'}%></span></p>
    <%= hidden_field_tag('value', '1') %>
  <%- else -%>
    <p><span class="notifylink"><i class="icon-envelope"></i><%= submit_tag "Don't notify me when this question has activity", {:id => '0', :class => 'btn'}%></span></p>
    <%= hidden_field_tag('value', '0') %>
  <%- end -%> 
<% end %>
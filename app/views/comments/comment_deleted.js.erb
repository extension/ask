<% if @parent_comment == true %>
  window.location.replace("<%= question_path(@question) %>");
<% else %>
  $("#comment-list").html("<%= escape_javascript(render partial: 'comments/comment_list', locals: {comment_list: @comments, parent_comment: nil}) %>");
<% end %>
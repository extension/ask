<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= (@page_title) ? @page_title + " — Ask an Expert — eXtension" : "Ask an Expert — eXtension" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body id="public">
  
  <%- if (Settings.app_location == 'demo') || (Settings.app_location == 'dev') %>
    <div id="app_location" class="demo">
      <p>You are viewing <strong>AaE 2.0 <%= Settings.app_location.upcase %></strong>. The data on this site is regularly deleted for development purposes. <strong><%= link_to("Visit Ask 2.0", "http://ask.extension.org") -%></strong>.</p>
    </div>
  <% end %>

  <div class="navbar navbar-fixed-top <%= 'demo' if (Settings.app_location == 'demo') || (Settings.app_location == 'dev') %>">
        <div class="navbar-inner">
          <div class="container">
            
            <% if current_user %>
              <span class="navavatar loggedin">
                <%= get_avatar(current_user, :thumb) %>
                <%= link_to expert_user_path(current_user.id) do %>
                   <%= current_user.name %>
                <% end %>
                &nbsp;
                <%= link_to 'Settings', expert_settings_profile_path() %>
                &nbsp;
                <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                &nbsp;
                <%= link_to("Expert Home", expert_home_path()) %>
              </span>  
            <% else %>
              <span class="navavatar">
                <%= link_to("Sign in", new_user_session_path(:redirect_to => request.fullpath) )%>
              </span>
            <% end %>
            
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <%= link_to("eXtension Ask an Expert", root_path(), :class => "brand") %>
            <div class="nav-collapse">
              <ul class="nav">
                <li class="active"><a href="">About</a></li>
              </ul>
            </div><!--/.nav-collapse -->
            <p><a href="" class="btn">Ask a Question</a></p>
          </div>
        </div>
        
      </div>

<div class="container-fluid">
  <%= flash_notifications -%>
  
  <div class="row-fluid">
    <%= yield %>
  </div>
  

</div>

<%= render(:partial => 'home/google_analytics') %>

</body>
</html>
